{
  "name": "@vibesafely/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VibeSafely MCP server — scan your app (including localhost) from Claude Code.",
  "bin": {
    "vibesafely-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@vibesafely/scan-core": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsup": "^8.3.0",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
